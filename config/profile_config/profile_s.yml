profile_name: "profile_s"
row_count: 40000

page_data:
  data_prep_page:
    data_file: "data_prep_page/profile_xs.xlsx"
    polling:
      interval_seconds: 15
      expected_ms: 220000       # 2 min — warn threshold
      max_wait_ms: 300000
    expected_sla:
      upload_max_ms: 40000
      download_max_ms: 25000

  baseline_page:
    data_file: "baseline_page/profile_xs.xlsx"
    polling:
      interval_seconds: 10
      expected_ms: 220000       # 2 min — warn threshold
      max_wait_ms: 400000
    expected_sla:
      upload_max_ms: 30000
      download_max_ms: 20000

  scoping_page:
    expected_sla:
      download_max_ms: 20000
    polling:
      interval_seconds: 10
      expected_ms: 220000       # 2 min — warn threshold
      max_wait_ms: 400000

  study_details_page:
    expected_sla:
      page_load_max_ms: 5000